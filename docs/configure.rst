.. _configure:

Configuration
=============

As pyimagediet cannot guess which tools you want to use or how, you need to provide that information. 


.. literalinclude:: ../pyimagediet/config.yml
    :language: yaml
